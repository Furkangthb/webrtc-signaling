services:
  webrtc-app:
    build: .
    container_name: webrtc-sunucu
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - .env
    volumes:
      - ./public/uploads:/app/public/uploads
      - ./kayitlar:/app/kayitlar